import { PricecalculationPipe } from './pricecalculation.pipe';

describe('PricecalculationPipe', () => {
  it('create an instance', () => {
    const pipe = new PricecalculationPipe();
    expect(pipe).toBeTruthy();
  });
});
